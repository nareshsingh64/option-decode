{
  "name": "@option-decode/trading",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@option-decode/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
